syntax = "proto3";

package scraper;

option go_package = "github.com/Adam-Aldave9/distributed-web-scraper/proto";

// ScraperService defines the communication between Supervisor and Worker
service ScraperService {
  // Restart worker (M --> W)
  // Graceful Shutdown Worker (M --> W)
  // Completion status (W --> M)
  // Register Worker (W --> M)
  // Health Heartbeat Stream (W --> M)

}


// messages